#include "Ircserv.hpp"


bool Ircserv::checkIfClientInServer(std::map<std::string, std::vector<int> > channelMap, std::string channel, int clientFd)
{
	// std::vector<int>::iterator it = std::find(_channels[channel].begin(), _channels[channel].end(), _clientFd);
	// if (it != channelMap[channel].end()) {
	// 	std::cout << "Cliente já está no canal." << std::endl;
	// } else {
	// 	std::cout << "Cliente não está no canal." << std::endl;
	// }
}